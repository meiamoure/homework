namespace Animals.Api.Constants;

public static class Routes
{
    private const string _prefix = "api";

    public const string Animals = $"{_prefix}/animals";

    public const string Owners = $"{_prefix}/owners";
}